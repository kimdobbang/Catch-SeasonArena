// src/config.ts

const config = {
  API_BASE_URL: "https://j11b106.p.ssafy.io",
};

// const devConfig = {
//   API_BASE_URL: "https://j11b106.p.ssafy.io",
// };

// const prodConfig = {
//   API_BASE_URL: "",
// };

// 수정
// const config = process.env.NODE_ENV === "production" ? prodConfig : devConfig;

export default config;
